{
  "name": "hl-design-system",
  "version": "1.0.0",
  "description": "HL 내부 운영 도구 UI 디자인 시스템",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean:build": "node scripts/prepare-build.mjs",
    "prebuild": "npm run clean:build",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "recharts": "^3.8.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
